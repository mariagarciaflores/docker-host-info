{
  "name": "docker-host-info",
  "version": "1.0.0",
  "description": "My first app using Docker",
  "main": "src/index.js",
  "devDependencies": {
    "jest": "^28.1.3",
    "supertest": "^6.2.4"
  },
  "scripts": {
    "test": "jest --forceExit --testTimeout=10000",
    "start": "node src/index.js"
  },
  "author": "Maria Griselda Garcia Flores",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1"
  },
  "jest": {
    "testEnvironment": "node",
    "coveragePathIgnorePatterns": [
      "/node_modules/"
    ]
  }
}
